spring:
  datasource:
    url: jdbc:h2:mem:zeeshop-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

security:
  jwt:
    secret: test-only-secret-not-for-production-1234567890abcdef
    access-token-expiration-ms: 900000
    refresh-token-expiration-ms: 604800000

admin:
  seed:
    username: admin
    email: admin@zeeshop.com
    password: Test1234!
